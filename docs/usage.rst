Basic Usage
===========

To use the CLI, run the following command:

.. code-block:: bash

   python cli.py [options]

For example, to generate a random password, you can use:

.. code-block:: bash

   python cli.py --generate-password

